<?xml version="1.0" encoding="utf-8"?>
<MotionScene xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:motion="http://schemas.android.com/apk/res-auto">

    <Transition
        motion:constraintSetEnd="@+id/end"
        motion:constraintSetStart="@id/start"
        motion:duration="50">

        <OnClick
            motion:clickAction="toggle"
            motion:targetId="@id/summary_text" />
    </Transition>

    <ConstraintSet android:id="@+id/start">
        <Constraint
            android:id="@+id/summary_text"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginTop="30dp"
            motion:layout_constraintEnd_toEndOf="@id/guideline_end"
            motion:layout_constraintHorizontal_bias="0.5"
            motion:layout_constraintStart_toStartOf="@id/guideline_start"
            motion:layout_constraintTop_toBottomOf="@+id/poster_image">
            <CustomAttribute
                motion:attributeName="maxLines"
                motion:customIntegerValue="4" />
        </Constraint>
    </ConstraintSet>

    <ConstraintSet android:id="@+id/end">
        <Constraint
            android:id="@+id/summary_text"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginTop="30dp"
            motion:layout_constraintEnd_toEndOf="@id/guideline_end"
            motion:layout_constraintHorizontal_bias="0.5"
            motion:layout_constraintStart_toStartOf="@id/guideline_start"
            motion:layout_constraintTop_toBottomOf="@+id/poster_image">
            <CustomAttribute
                motion:attributeName="maxLines"
                motion:customIntegerValue="20" />
        </Constraint>
    </ConstraintSet>
</MotionScene>